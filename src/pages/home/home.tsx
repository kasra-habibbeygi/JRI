// Components
import { FAQ, Header, Versions } from './components';

const Home = () => {
    return (
        <main>
            <Header />
            <Versions />
            <FAQ />
        </main>
    );
};

export default Home;
