// Components
import { Contracts, Header, Security, Trust } from './components';

const CoolVersion = () => {
    return (
        <main>
            <Header />
            <Contracts />
            <Security />
            <Trust />
        </main>
    );
};

export default CoolVersion;
