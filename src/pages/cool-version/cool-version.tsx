// Components
import { Contracts, Header } from './components';

const CoolVersion = () => {
    return (
        <main>
            <Header />
            <Contracts />
        </main>
    );
};

export default CoolVersion;
